{
  "name": "brief5",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && parcel build index.html --dist-dir dist"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "parcel": "^2.8.3"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3",
    "sass": "^1.62.1",
    "typescript": "^4.0.0"
  },
  "parcel": {
    "entries": {
      "index": "index.html"
    },
    "outputs": {
      "main": "dist"
    }
  }
}

